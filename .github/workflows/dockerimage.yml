name: Docker Image CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:    
#    - name: Docker Login
#      uses: Azure/docker-login@v1
#      with:
#        login-server: ${{ secrets.DOCKER_CONTAINER_REGISTRY }}
#        username: ${{ secrets.DOCKER_USERNAME }} 
#        password: ${{ secrets.DOCKER_PASSWORD }}
    - name: Xpto
      id: Abc
      run: |
        echo "$(ls)"
        
    - name: Xpto_1
      id: Abc_1
      run: |
        echo "$(ls ../)"
    
    - name: Xpto_2
      id: Abc_2
      run: |
        echo "$(ls ../../)"
    
    - name: Build e Push no Azure Container Registry
      run: docker build --tag springdemo.azurecr.io/spring-boot-app:latest .

